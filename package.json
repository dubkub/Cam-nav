{
  "name": "cam-nav",
  "private": true,
  "version": "0.1.0",
  "description": "Surveillance-aware navigation: route around ALPRs, ticket cameras and other fixed surveillance, with a tunable time-vs-privacy tradeoff.",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20.11" },
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build && pnpm --filter @cam-nav/api run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "api": "pnpm --filter @cam-nav/api run dev",
    "ingest": "pnpm --filter @cam-nav/data run ingest",
    "app": "pnpm --filter @cam-nav/mobile run start",
    "app:web": "pnpm --filter @cam-nav/mobile run web"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
